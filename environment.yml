#conda env update -p ./env
name: lclf
dependencies:
  - python=3.7.*
  - pip:
    - confluent_kafka
    - requests
    - fastavro
    - faust
    - httpx
    - python-schema-registry-client
    - cassandra-driver
    - time
    - avro
    - aiokafka